@model List<ResultTopDiscountDto>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/AdminLayout/Index.cshtml";
}

<section class="content container-fluid">
    <div class="row">

        <div class="col-md-12">
            <div class="chart-box">
                <div class="bs-example" data-example-id="bordered-table">
                    <table class="table table-bordered">
                        <thead>
                            <tr>
                                <th>Özel İndirim Başlığı</th>
                                <th>Özel İndirim Alt Başlığı</th>
                                <th>Sil</th>
                                <th>Güncelle</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <th scope="row">@item.Title</th>
                                    <td>@item.SubTitle</td>
                                    <td><a href="/Admin/TopDiscount/DeleteTopDiscount/@item.TopDiscountID" class="btn btn-danger">Sil</a></td>
                                    <td><a href="/Admin/TopDiscount/UpdateTopDiscount/@item.TopDiscountID" class="btn btn-success">Güncelle</a></td>
                                </tr>
                            }
                        </tbody>
                    </table>
                    <a href="/Admin/TopDiscount/CreateTopDiscount/" class="btn btn-primary">Yeni Özel İndirim Girişi</a>
                </div>
            </div>
        </div>
    </div>
</section>

